{
  "watch": ["src", ".env.development", "tsconfig.json"],
  "ext": "js,ts,json",
  "ignore": ["src/logs/*", "src/**/*.{spec,test}.ts"],
  "exec": "npm run lint && ts-node -r tsconfig-paths/register --transpile-only src/index.ts"
}
